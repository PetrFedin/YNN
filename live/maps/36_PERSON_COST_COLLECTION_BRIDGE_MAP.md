# Map 36 — Person-cost → collection gap close

```
MOKEEVA / ZHUKOVA / MERKUSHINA / EMBROIDERY
        │ model № (43-xx, 0-xxxx, …)
        ▼
 person_cost_collection_bridge
        │
        ├── CLOSES_COLLECTION_GAP  → HIGH owner confirm (not Accept)
        ├── SUPPORTS_COLLECTION
        ├── GOODS_COST_ONLY
        └── ORPHAN_COST
```

Rule: workshop card = **cost proxy evidence**, never auto-Accept into SoT.
